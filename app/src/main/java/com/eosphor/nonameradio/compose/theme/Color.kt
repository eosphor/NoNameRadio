package com.eosphor.nonameradio.compose.theme

import androidx.compose.ui.graphics.Color

// Основные цвета из colors.xml
val Primary = Color(0xFF125688)
val PrimaryDark = Color(0xFF232325)
val Accent = Color(0xFF1d6293)
val AccentDark = Color(0xFF1d6293)

// Цвета фона
val WindowBackground = Color(0xFFFFFFFF)
val WindowBackgroundDark = Color(0xFF2c2c2e)

// Цвета плеера
val PlayerBackground = Color(0xFFFFFFFF)
val PlayerBackgroundDark = Color(0xFF232325)
val PlayerShadow = Color(0xFFe5e5e5)
val PlayerShadowDark = Color(0xFF000000)

// Цвета текста
val TextColorPrimary = Color(0xFFFFFFFF)
val TextColorSecondary = Color(0xFF777777)

// Цвета табов
val TabUnderline = Color(0xFFe0347f)
val TabUnderlineDark = Color(0xFF1d6293)

// Цвета тегов
val TagBackground = Color(0xFFE0E0E0)
val TagBackgroundDark = Color(0xFF212121)

// Цвета меню
val MenuTextSelected = Color(0xFF125688)
val MenuTextSelectedDark = Color(0xFFFFFFFF)
val MenuTextDefault = Color(0xFF000000)
val MenuTextDefaultDark = Color(0xFFFFFFFF)

// Цвета нижнего меню
val MenuBottomTextSelected = Color(0xFFFFFFFF)
val MenuBottomTextSelectedDark = Color(0xFFFFFFFF)
val MenuBottomTextDefault = Color(0xFFe1e1e1)
val MenuBottomTextDefaultDark = Color(0xFFd7d7d7)
val MenuBottomBackground = Color(0xFF1d6293)
val MenuBottomBackgroundDark = Color(0xFF1a1a1b)

// Цвета иконок
val IconsInItemBackground = Color(0xFF000000)
val IconsInItemBackgroundDark = Color(0xFFFFFFFF)

// Цвета для swipe delete
val SwipeDeleteIcon = Color(0xFFFFFFFF)
val SwipeDeleteIconDark = Color(0xFF2c2c2e)
val SwipeDeleteBackground = Color(0xFFee0000)
val SwipeDeleteBackgroundDark = Color(0xFFdd0000)

// Цвет записи
val StartRecordingColor = Color(0xFFff3333)

// Цвет кнопки SnackBar
val SnackBarButtonColor = Color(0xFFD500F9)